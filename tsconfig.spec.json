/* To learn more about this file see: https://angular.io/config/tsconfig. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": [
      "jasmine"         // Included to prevent errors in IDEs
    ]
  },
  "include": [
    "src/**/*.spec.ts",   // Included to prevent errors in IDEs
    "src/**/*.d.ts"       //
  ]
}
